# Life K-Line Final · Three Product Masters

从本版本开始，人生 K 线正式拆分为三个相互独立的产品母版。以后不要再从旧 `lifekline` 或旧客户页面直接开始。

## 1. Mobile HTML
路径：`products/mobile-html/`

手机优先的单文件离线 HTML。核心是触摸手势、捏合缩放、横屏 K 线工作台。手机版保持已经封版的移动端交互，不为了和电脑版统一而改回桌面逻辑。

## 2. Desktop HTML
路径：`products/desktop-html/`

电脑优先的单文件离线 HTML。正式母版采用原 `main/2002-12-12/`「壬午女命 · 可解释运势 K 线」的界面和操作逻辑：年份双滑块、指标卡、左图右详情、点击选年、评分口径对比、年度评分账本、规则说明、JSON / CSV 导出。

电脑版不要套用手机版的捏合、横屏工作台或手机卡片布局。源码允许拆分维护，但最终客户交付必须打包成一个自包含 HTML。

## 3. PDF
路径：`products/pdf/`

正式档案交付版本，可直接发送、保存和打印。

## 共同原则

- 三个版本共享同一命盘与年度数据口径，但 UI / 交互 / 排版分别维护。
- 两个 HTML 最终产物必须是单文件、自包含、无 CDN、无外链 CSS / JS、核心页面无主动网络请求。
- HTML 的运行不依赖 GitHub，因此中国大陆网络、VPN 与否不影响本地打开。
- GitHub 只保存母版源码和版本历史，不再承担客户页面运行时托管。
- 用户没有明确要求时不调用 AskLingxi；真太阳时默认自行计算。
- 新客户必须重新计算真太阳时、四柱、大运、流年和评分，不能直接复用示例命盘的数据。
