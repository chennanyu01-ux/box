# Life K-Line Final Workflow v2

## 目标

使用 `life-kline-final` 分支中的三个正式产品母版，为新的命主一次计算、分别输出 Mobile HTML、Desktop HTML 和 PDF。三个产品共享同一份客户数据，但界面、交互和排版独立维护。

## 强制规则

- 绝对不要回到旧 `lifekline`、根目录旧网页或 `2002-12-12/` 示例作为新任务起点。
- 用户没有明确要求调用 AskLingxi 时，不调用 AskLingxi。
- 真太阳时默认自行计算：经度修正 + 当日均时差；时间范围必须判断整个范围是否跨时辰或跨日。
- 四柱、大运、十神、天乙贵人、命局取向和年度评分必须按新命主重新计算，不能只改姓名或标题。
- 客户数据只计算一次，写入统一 JSON，再分别交给三个产品渲染器，禁止三套产品各算一遍造成结果不一致。
- 界面中的第四项统一叫“贵人”，不再使用模糊的“机会”。
- 100 年只是展示范围，不用于寿命推断；分数是模型相对指数，不包装成现实概率。

## 输入

至少需要：出生日期、出生时间、性别、出生地；姓名用于产品标题和文件命名。

## 统一数据流程

1. 根据出生地经度与当天均时差计算真太阳时，并确认是否跨时辰、跨日。
2. 排定四柱。
3. 计算大运方向、起运点和大运序列。
4. 根据日主动态建立十神映射，不使用任何示例命盘的硬编码十神。
5. 生成 100 年流年与交运分段，计算综合年度 K 线和年度说明。
6. 生成个人画像文字和相关 10 分制标签。
7. 按 `products/INPUT_SCHEMA.md` 写成一份客户 JSON，作为三个产品的唯一数据源。

## 三个产品母版

### Mobile HTML
目录：`products/mobile-html/`

正式版本：Life K-Line Mobile v1.0。

生成器把 template / styles / app 与客户 JSON 合并成一个完全自包含的 HTML 文件。核心交互包括：单指横向拖动、双指连续捏合缩放、轻触年份、双击恢复百年总览、横屏行情工作台、综合 / 事业 / 财运 / 感情 / 贵人五线。

生成后必须检查：
- 无 CDN、无 `<script src>`、无外部样式、无 fetch/XHR 或其他远程资源；
- SVG 的 `http://www.w3.org/2000/svg` 只是命名空间，不属于网络依赖；
- 本地 `file://` 打开时主图、手势、横屏、年份详情和筛选正常。

### Desktop HTML
目录：`products/desktop-html/`

正式版本：Life K-Line Desktop v1.0。

同样输出单文件离线 HTML，但针对电脑宽屏和鼠标操作优化：鼠标左键拖动时间轴、滚轮围绕指针缩放、点击年份、双击恢复百年、全屏查看；触控屏仍兼容双指缩放。

最终 HTML 直接由本地浏览器运行，不依赖 GitHub、服务器或互联网，所以中国大陆网络、VPN 与否不影响核心页面。

### PDF
目录：`products/pdf/`

正式版本：Life K-Line PDF v1.0。

作为正式档案 / 打印 / 备份交付，保留人生 K 线总览、时间结构、当前窗口、峰谷索引、个人画像、命盘底图和完整逐年索引。生成成功后必须逐页渲染检查，而不是只看程序是否退出成功。

## 推荐生成命令

- Mobile HTML：`python products/mobile-html/build.py --data customer.json --out 客户名_人生K线_手机版.html`
- Desktop HTML：`python products/desktop-html/build.py --data customer.json --out 客户名_人生K线_电脑版.html`
- PDF：`python products/pdf/build_pdf.py --data customer.json --out 客户名_人生K线个人图谱.pdf`

## 固定评分语义

综合分 = 事业 30% + 财运 30% + 感情 20% + 贵人 20%。

十神基础向量：
- 比肩 `[1,-2,0,2]`
- 劫财 `[0,-3,-1,3]`
- 食神 `[4,3,1,2]`
- 伤官 `[5,2,-1,1]`
- 偏财 `[2,6,2,2]`
- 正财 `[2,5,4,1]`
- 七杀 `[3,0,-2,0]`
- 正官 `[5,1,1,2]`
- 偏印 `[3,0,0,5]`
- 正印 `[3,1,1,5]`

分项线必须根据新命主日主动态映射这些十神向量，再围绕已经计算好的综合年度分校准；不能沿用某个示例人的甲木、壬水等固定映射。

## 交付原则

标准数字交付优先保留三份：手机版 HTML、电脑版 HTML、PDF。两份 HTML 是本地离线产品，PDF 是永久档案和兼容性兜底。GitHub 只保存源码与版本历史，不作为客户运行页面的必要条件。

## 固定触发语

当用户说“用人生K线最终版生成”“用最终版人生K线”“按我的人生K线母版生成”时，优先读取 `README_FINAL.md`、本文件、`PRODUCTS.md`、`products/INPUT_SCHEMA.md` 与对应产品目录，不使用旧版。
