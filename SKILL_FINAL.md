# Life K-Line Final Workflow

## 目标

使用 `life-kline-final` 分支中的最终网页母版，为新的命主生成一份独立、可解释、可转发的人生 K 线网页。

## 强制规则

- 绝对不要回到旧 `lifekline` 仓库作为生成起点。
- 用户没有明确要求调用 AskLingxi 时，不调用 AskLingxi。
- 真太阳时默认自行计算：经度修正 + 当日均时差；若用户给的是时间范围，要判断整个范围是否跨时辰。
- 四柱、大运、十神、天乙贵人、评分口径必须按新命主重算，不能只改标题。
- 页面结构、视觉、K 线、分项趋势、年度账本、评分口径切换、JSON/CSV 导出、复制链接等功能沿用最终母版。
- 100 年只是展示范围，不用于寿命推断；分数是模型相对指数，不包装成现实概率。

## 输入

至少需要：出生日期、出生时间、性别、出生地。

## 生成流程

1. 计算出生地真太阳时，并确认是否跨时辰。
2. 排定四柱。
3. 计算大运方向、起运点与大运序列。
4. 根据日主重建十神映射，不沿用示例命盘的硬编码。
5. 根据命局设置三种可解释评分口径：主口径、中性口径、对照口径。
6. 生成 100 年流年与交运分段。
7. 以 `2002-12-12/` 下的已验证源码和根目录 `styles.css` 为 UI/交互基准，替换新命主的数据与文字。
8. 检查页面渲染、年份切换、K 线、四项趋势、年度账本、口径切换、JSON/CSV 和复制链接。
9. 将成品部署到 `main` 分支的独立目录，并返回可转发 URL。

## 固定评分语义

综合分 = 事业 30% + 财运 30% + 感情 20% + 贵人/机会 20%。四项从 50 分起算。

十神基础向量：
- 比肩 `[1,-2,0,2]`
- 劫财 `[0,-3,-1,3]`
- 食神 `[4,3,1,2]`
- 伤官 `[5,2,-1,1]`
- 偏财 `[2,6,2,2]`
- 正财 `[2,5,4,1]`
- 七杀 `[3,0,-2,0]`
- 正官 `[5,1,1,2]`
- 偏印 `[3,0,0,5]`
- 正印 `[3,1,1,5]`

固定权重与关系系数以当前 `2002-12-12/model.js` 为准，包括：透干/藏干权重、流年 1.4、大运 1.0、当年×大运 0.8、大运×原局 0.6、日支感情项 ×1.5。

## 固定触发语

当用户说“用人生K线最终版生成”“用最终版人生K线”“按我的人生K线母版生成”时，优先读取本文件和 `README_FINAL.md`，不要使用旧版。
